# functional-decomposition-simulation
Multi-threaded simulation of levels of grain, deer, and tigers based on temp and precipitation

To run simulation:
`g++ -o project-3 project-3.cpp -lm -fopenmp`
 `project-3`
 
